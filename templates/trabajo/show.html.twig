{% extends 'base.html.twig' %}

{% block title %}Trabajo{% endblock %}

{% block body %}
    <h1>Trabajo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ trabajo.id }}</td>
            </tr>
            <tr>
                <th>Registro</th>
                <td>{{ trabajo.registro }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ trabajo.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('trabajo_index') }}">back to list</a>

    <a href="{{ path('trabajo_edit', {'id': trabajo.id}) }}">edit</a>

    {{ include('trabajo/_delete_form.html.twig') }}
{% endblock %}
