<form method="post" action="{{ path('trabajo_delete', {'id': trabajo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trabajo.id) }}">
    <button class="btn">Delete</button>
</form>
